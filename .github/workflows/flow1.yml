on:
  workflow_call:
    inputs:
      repo_name:
        required: true
        description: "name of trigger repository"
        type: string

jobs:
  flow1:
    runs-on: ubuntu-latest
    steps:
      - name: simple job
        run: |
          echo "test"
          echo ${{ inputs.gitlab-project-id }}
